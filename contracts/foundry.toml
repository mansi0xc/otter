[profile.default]
src = "src"
test = "test"
out = "out"
libs = ["lib"]
solc = "0.8.26"
evm_version = "cancun"
via_ir = true
optimizer = true
optimizer_runs = 200
ffi = true
fs_permissions = [{ access = "read", path = "../fixtures" }]

[profile.default.fuzz]
runs = 512
